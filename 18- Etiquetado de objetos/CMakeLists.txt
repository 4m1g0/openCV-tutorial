cmake_minimum_required(VERSION 2.8)
project( Etiquetado )
find_package( OpenCV REQUIRED )
include_directories( ${OpenCV_INCLUDE_DIRS} )
add_executable( Etiquetado Etiquetado.cpp )
target_link_libraries( Etiquetado ${OpenCV_LIBS} )
