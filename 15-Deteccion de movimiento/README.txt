For this example I had to download Opencv version 3.1.0 from github repository because API has changed after 2.4 regarding to BackgroundSustractors.

Setup process:
* install dependencies( incluiding eigen 3.0)
* mkdir build
* cd build
* cmake ../
* make
* sudo make install
